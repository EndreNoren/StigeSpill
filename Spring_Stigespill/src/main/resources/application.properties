spring.application.name=Spring_Stigespill

# ==========================================
# hjemmenett (H2 Database) - KOMMENTERT UT
# ==========================================
# spring.datasource.url=jdbc:h2:mem:stigespill
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# For H2 lager vi tabeller og kjorer data.sql hver gang
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.sql.init.mode=always


# ==========================================
# Skolenettet (PostgreSQL) - AKTIV
# ==========================================
spring.datasource.url=jdbc:postgresql://ider-database.westeurope.cloudapp.azure.com:5433/h186505
spring.datasource.username=h186505
spring.datasource.password=polse

# 'update' sørger for at tabellene ikke slettes, men data.sql kan fylle dem
spring.jpa.hibernate.ddl-auto=update

# 'always' gjør at data.sql kjører hver gang appen starter
spring.sql.init.mode=always


# ==========================================
# FELLES INNSTILLINGER
# ==========================================
spring.jpa.show-sql=true

# Sørger for at Hibernate lager tabellene FØR data.sql prøver å sette inn data
spring.jpa.defer-datasource-initialization=true